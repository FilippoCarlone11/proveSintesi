ERRORI TROVATI

- se il circuito ha un segnale nominato test yosys non riesce a fare la scan chain
- dobbiamo trovare un modo per generalizzare il nome del clock e del reset
- se ci sono 2 moduli con reset e/o clock diversi nel case la scan fallisce